version: '2'
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/schema"
    gen:
      go:
        out: "internal/database"
    database:
      # uri: "${DATABASE_URL}"
      uri: "postgres://postgres:postgres@localhost:5432/devtube"
